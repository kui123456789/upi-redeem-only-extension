interface:
  display_name: "Upi Redeem Only Extension"
  short_description: "Repo-specific patterns and workflows for upi-redeem-only-extension"
  default_prompt: "Use the upi-redeem-only-extension repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true